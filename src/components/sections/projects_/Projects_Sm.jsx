import { motion } from 'framer-motion'
import { Tabs } from '@components'

const Projects_Sm = () => {
    return (
        <motion.div className="m-2 overflow-hidden md:hidden">
            <Tabs />
        </motion.div>
    )
}
export default Projects_Sm
